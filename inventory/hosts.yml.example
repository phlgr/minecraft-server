---
minecraft_servers:
  hosts:
    minecraft-server-1:
      ansible_host: 192.168.1.100
      ansible_user: ubuntu

      ansible_port: 22
      
      # ansible_ssh_private_key_file: Path to your SSH private key (if using key-based auth)
      # Uncomment and set this if you're using SSH keys instead of passwords
      # ansible_ssh_private_key_file: ~/.ssh/id_rsa
      
      # If using password authentication instead of keys:
      # You'll need to use --ask-pass flag when running ansible-playbook
      # Or use ansible-vault to encrypt passwords (more secure)
